#!/bin/bash

if command -v conky >/dev/null 2>&1; then
    "conky-toggle"
else
    x-terminal-emulator --title='Conky Installer' -e bash -c "conky-installer" &
    notify-send --urgency low "Please Install Conky"
fi
